1. 클래스를 사용하는 시점에서 딱 한번 메모리에 로딩하기 위해서 사용하느는 키워드는 무엇인가.
    -> static
2. 다음은 JVM에서 사용하는 메모리 중 어떤 메모리를 설명하고 있는지 쓰세요.
    1. 메서드가 호출되면 호출된 기계어코드가 push되고 실행되는 메모리 공간
    2. 현재 프로그램이 실행되고 있는 상태를 파악할 수 있다.
    3. LIFO(Last-In-First-out) 구조이다.
3. 객체생성을 막으려면 어떻게 하면 되는가?
    -> 생성자를 private으로
4. 객체생성에 의해 메모리에 만들어진 객체를 무엇이라고 하는가?
    -> 인스턴스
5. JVM이 사용하는 메모리 4가지를 모델을 쓰세요.
    -> Method Area, Stack Area, Heap Area, Literal POOL